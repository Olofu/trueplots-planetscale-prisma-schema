// Schema for a simple URL shortener
// This schema tracks short links by associating them to a user
// url      = env("DATABASE_URL")

generator client {
  provider = "prisma-client-js"
  previewFeatures = ["referentialIntegrity"]
}

datasource db {
provider = "mysql"
  url      = "mysql://4rhirf3nrjeh:pscale_pw_oGS-xow_A0VptQ59PzqOs_fmwKIXWBcNqBAaVw4KZVE@apj49n4h2nnq.us-east-2.psdb.cloud/trueplots-planetscale-prisma-schema?sslaccept=strict
"
  referentialIntegrity = "prisma"
}

model Link {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  url       String @db.Text
  shortUrl  String @db.Text
  userId    String? @db.Text
  User      User?    @relation(fields: [userId], references: [id])
}

model User {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  name      String? @db.Text
  email     String @db.Text
  links     Link[]
}

model Post {
  id        String      @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  title     String   @db.VarChar(255)
  content   String? @db.Text
  published Boolean  @default(false)
  author    String   @db.Text
  authorId  Int
}
